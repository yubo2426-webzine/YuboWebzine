여백 원인이 보입니다. 화면을 목록 55% / 지도 45%로 고정 분할해두었는데, 모바일에서는 지도 영역이 흰 공백으로 남아 그 자리를 통째로 낭비하고 있습니다.

**모바일에서는 둘을 나누지 말고 하나씩 전체 화면으로 쓰는 게 맞습니다.** `목록` / `지도` 전환 버튼을 넣어서, 목록을 고르면 화면 전체가 목록이 되게 했습니다.

구문 오류 없이 빌드됩니다.

`src/components/ResourceMap.tsx`를 이 파일로 덮어쓰면 됩니다.

## 바뀐 점

**모바일: 목록 / 지도 전환** — 상단에 버튼 두 개가 생겼습니다. `목록`을 누르면 화면 전체가 목록이라 한 번에 5~6건이 보이고, `지도`를 누르면 지도가 화면을 다 씁니다. 반씩 나눠 쓰던 구조를 없앴으니 빈 공백도 사라집니다. 목록 버튼에는 현재 건수가 함께 표시됩니다.

**높이 계산 수정** — `100vh`를 `100dvh`로 바꿨습니다. 모바일 브라우저는 주소창이 접혔다 펴지는데, `100vh`는 주소창이 없는 상태 기준이라 실제 화면보다 커집니다. 아래쪽이 잘리거나 남는 원인 중 하나였습니다.

**지도 크기 재계산** — 지도를 숨겼다 다시 켜면 카카오맵이 크기를 0으로 인식해 타일이 깨집니다. 탭을 되돌아올 때 `relayout`을 호출하도록 했습니다.

**경로 탐색 후 자동 전환** — 목록 화면에서 「지도에서 경로 보기」를 누르면 경로가 그려진 뒤 지도 탭으로 자동으로 넘어갑니다. 안 그러면 경로를 그려놓고도 볼 수 없습니다.

PC 화면은 그대로 좌우 분할입니다.

---

배포 후 휴대폰에서 확인해 주세요.

- 목록 탭에서 아래 여백 없이 카드가 채워지는지
- 지도 탭으로 갔다가 목록으로 돌아온 뒤 다시 지도를 눌렀을 때 지도가 정상인지
- 기관을 눌렀을 때 상세정보가 뜨고 「목록」으로 돌아가는지
